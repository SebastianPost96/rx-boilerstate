import { TileImagePipe } from './tile-image.pipe';

describe('TileclassPipe', () => {
  it('create an instance', () => {
    const pipe = new TileImagePipe();
    expect(pipe).toBeTruthy();
  });
});
